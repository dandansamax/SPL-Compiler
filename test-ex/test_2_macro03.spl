/*
this is used to 
test duplicate macro
*/

#define DUPLICATE int
#define DUPLICATE float

DUPLICATE test_1_r07()
{
  DUPLICATE a, b, c;
  a = 1;
  b = a && 2;
  c = b || 3;
  a = c | 4;
  b = a & 5;
}