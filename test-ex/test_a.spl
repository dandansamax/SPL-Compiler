int mod(int a, int b){
    return a - (a / b) * b;
}
int gcd(int a, int b){
    if (b) 
        return gcd(b, mod(a, b)); 
    else
        return a;
}
int main()
{
    int a, b;
    a = read();
    b = read();
    write(gcd(a, b));
    return 0;
}