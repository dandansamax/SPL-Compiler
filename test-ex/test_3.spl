int main()
{
    {
        float n;

        if (n) {} // Error type 16
    }

    {
        struct st
        {
            int a, b;
        } s[10];

        char x, y, z;

        if (s[0] > s[1]) {} // Error type 17
       
        z = (x + y);
        z = (s[0] + s[1]); // Error type 18
    }

    {
        struct undefined_st s2; // Error type 19
    }
}