/*
This test case is used to test duplicate macro.
*/

#define DUPLICATE int
#define DUPLICATE float

DUPLICATE test_5()
{
  DUPLICATE m = 1;
  float n = 2.2;
  DUPLICATE x[5], y[10];
  x[m] = 7;
  y[n] = 8;
  return x[y];
}