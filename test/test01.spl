int mod(int a, int b){
    return a - (a/b)*b;
}
int gcd(int c,int d){
    if (d==0) 
        return c;
    else
        return gcd(d,mod(c,d));
}
int main()
{
    int n, m;
    n = read();
    m = read();
    write(gcd(n,m));
    return 0;
}